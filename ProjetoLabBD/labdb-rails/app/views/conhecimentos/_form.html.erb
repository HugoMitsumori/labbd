<%= form_for(conhecimento) do |f| %>
  <% if conhecimento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conhecimento.errors.count, "error") %> prohibited this conhecimento from being saved:</h2>

      <ul>
      <% conhecimento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="field">
    <%= f.label :descricao %>
    <%= f.text_field :descricao %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
