<%= form_for(follower) do |f| %>
  <% if follower.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(follower.errors.count, "error") %> prohibited this follower from being saved:</h2>

      <ul>
      <% follower.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :follower_login %>
    <%= f.text_field :follower_login %>
  </div>

  <div class="field">
    <%= f.label :followed_login %>
    <%= f.text_field :followed_login %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
