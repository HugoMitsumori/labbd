= form_for @add_compan_to_contractor do |f|
  - if @add_compan_to_contractor.errors.any?
    #error_explanation
      %h2= "#{pluralize(@add_compan_to_contractor.errors.count, "error")} prohibited this add_compan_to_contractor from being saved:"
      %ul
        - @add_compan_to_contractor.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :company_id
    = f.number_field :company_id
  .actions
    = f.submit 'Save'
