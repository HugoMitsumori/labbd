%h1 Listing services

%table.table
  %thead
    %tr
      %th Author
      %th Service name
      %th Start date
      %th End date
      %th Status
      %th Price
      %th Final score
      %th


  %tbody
    - @services.each do |service|
      %tr
        %td= User.find(service.user_id).login
        %td= service.service_name
        %td= service.start_date
        %td= service.end_date
        %td= service.status
        %td= service.price
        %td= service.final_score
        %td= link_to 'View', service

%br

= link_to 'New Service', new_service_path
